# Milestone 031 — Commission the Job

## Status and authorization

**Implementation candidate delivered; independent review and human comprehension pending.** Matt accepted the
review's P1 and five P2 findings, supplied two owner rulings, and authorized the fully revised scope
in this task. The authorization was to implement this milestone, verify and commit it, then stop for independent review.
Milestone 030 remains operationally closed through its bounded owner exception.

## Feature claim

From the baseline opening Vincent selects an offered SecureTribute target/method, then himself or
Tommy, reviews the plan, and confirms once. Direct delegation creates one operation with Vincent as
owner and Tommy as initial executor, without a temporary personal start. An owner already executing
work may commission a free known subordinate; execution capacity and supervision are separate.
Qualified autonomous actors must reach the same commissioning path now, not eventually.

## Owner rulings and bounded scope

- Preserve milestone 029's exact activation, attention limits, ordering, salience, late feasibility
  and no-backfill behavior before executor selection. Separate commissioning eligibility from
  personal-execution eligibility without multiplying target/method/executor candidates.
- Only after an eligible SecureTribute leaf is selected, open a paused replayable draft while the
  original prepared decision remains unresolved. Backtracking cannot deliberate again, consume RNG,
  increment decision counts, reserve resources, disclose information, advance time or choose inaction.
  Return to the same unresolved decision; confirmation validates and commits exactly once through
  shared resolution. Invalid/repeated confirmation must have no partial effects.
- Executor stage: protected self when personally eligible plus up to five known eligible direct
  subordinates; never a sixth subordinate. Reuse delegation eligibility, salience, relationships and
  capability beliefs with no new coefficients. Deterministic retention ties; neutral candidate-id
  presentation. NPCs evaluate the same retained options. Revalidate at commit, including capacity,
  acquaintance, direct subordination, access and duplicate owned work. No global target reservation.
- SecureTribute means a multi-phase pressure operation. Display a static qualitative expectation:
  "Usually unfolds over several days; setbacks may extend it." Never derive a live forecast from
  private phase, retries, resistance, skills, pending events or completion times. Preserve timings.
- Reuse milestone 030's bounded assignment briefing: capture held refusal and any held affirmative
  vulnerability of that target at confirmation, preserving stance, confidence, claimed basis/order;
  deliver only to the recipient via ordinary receipt effects. No unrelated knowledge or later reread.
- Preserve stable owner/local identity, target, assignment, constraints, ordered versus live method,
  responsibility, progress, advance ordinals, scheduling, commitments and cash-recipient rules.
- Owner known status must be operation-specific: unrelated/other-operation accounts and private
  progress cannot update it. Use existing vocabulary and bounded attribution; ambiguity stays an
  ordinary account. Owner-observable receipts remain valid. Explain staffing using known assignments
  only; authoritative eligibility never grants unknown assignment details.
- Preserve executor continuation/alteration/postponement, owner cancellation/reassignment, scheduled
  and information-triggered reviews, event validation and cleanup. Unchanged review never delays work.
- Reconstruct draft selections, retained evaluations, captured briefing, execution/review events and
  character-specific knowledge through existing same-build replay persistence. No new save system.

## Required proof and verification

Natural public-session proof: Grocery/Threaten/Tommy directly, confirmation, owner bandwidth, second
staffing result, scheduled execution and source-limited consequence. Staged proofs cover busy owners,
six-option bounds and protected self, deterministic ties, no product search, unknown/busy executors,
stale/duplicate confirmation, no global target reservation and present autonomous reachability.

Replay fresh processes before executor selection, after selection, before confirmation, after back,
during execution and before information delivery. Backtracking preserves counts/RNG/evaluations and
creates no work. Compare scheduling, identities, commitments, responsibility, progress and projections.
Negative controls: wrong recipient, absent assessment, unrelated beliefs/accounts/operations, later
issuer change, private method/failure and unknown staffing. Positive controls: qualifying account or
own receipt. Mutate hidden time/resistance without changing expectation. Test existing control/review
paths, isolated cancellation/reassignment, idempotent collection and advancement-pattern equivalence.

Run full relevant build/test, Runner variants/compare/viewpoints, Godot commissioning and restart
save/load gates. Mutation-check shortcut start/handoff, NPC self-only, repeated preparation, product
search, objective evaluation, forecast leakage, belief copying, any-report status, private method,
review rescheduling and lost draft. Record actual results, limits and first divergences honestly.

Human comprehension gate: without coaching Matt can identify objective, executor, ordered method,
multi-day scope, known progress and staffing reasons. No-report is not confirmed inactivity and the
expectation is not a guaranteed date. Coarse expectation inadequacy requires separate scope, not leaks.
Unchanged autonomous winner/current duration/no natural intervention are acceptable; absent NPC
reachability, lost controls, draft effects, leakage or broken replay are not.

## Exclusions and carried work

No tuning, RNG/fixture/trait/resistance changes, generalized planner, new strategy, UI framework or
persistence architecture. Defer new intervention mechanics, delayed commands, new refusal/negotiation,
live forecasts and new reporting vocabulary. Rival organization is a future separately authorized
content priority, not part of 031. No police, politics, map, tactical or territory systems.

OPEN_CONCERNS #8 receives a semantic ruling but pacing remains provisional; #9 is the commissioning
target. Existing tuning, calendar engineering and decision-RNG concerns remain. Milestone 030's
independent audit remains retrospective. Existing ROADMAP/proposal edits are unrelated user work;
leave them untouched and uncommitted. No subsequent milestone may begin before review and acceptance.

## Implementation delivered

Implemented by Codex in `codex/milestone-031-commission-job`, based on `ae4e465` (Milestone 030
closure), whose implementation parent is `5874077`. The implementation commit is the commit that
adds this archive; no self-referential hash or independent acceptance is asserted here.

- `Commissioning` caches the executor evaluation after a retained SecureTribute leaf. Retention
  filters existing eligibility and salience, orders by salience then the sum of existing delegation
  relationship/capability considerations, breaks ties by candidate id, and keeps at most five
  delegates. Eligible self is added independently. `Utility.DelegationConsiderations` is shared by
  retention and full evaluation, with the existing coefficients and capability ladder unchanged.
  Only retained alternatives receive full scoring/noise. A keyed `ForOccasion` stream gives each
  decision/leaf stable draws without consuming the first-stage stream or world counters. Cache hits
  return the same evaluation; a caller cannot replace the retained leaf's fields through its id.
- `SimulationSession` holds the unresolved decision, selected leaf and optional executor. Existing
  `Choose` commands record leaf, executor, back and confirmation inputs. `PersistentSession` needs
  no new command type, schema, serialization system or delayed command. Real Godot buttons expose
  the same draft. Confirmation calls the shared `Pipeline.Resolve`; automatic resolution prepares
  and evaluates the identical executor alternatives now.
- `Commit` validates before writing and creates the initial owner/executor association directly.
  It retains assignment, deadline, ordered method and responsibility, creates the usual commitments,
  and schedules the original strategy step/review paths. There is no temporary personal start or
  replacement of the owner's existing execution. Owner review, reassignment, cancellation and
  executor control continue through their existing paths.
- `AssignmentBriefing` extracts the existing bounded target assessment and receipt effects. Initial
  delegation captures the target refusal and held affirmative assessment at confirmation, retaining
  the issuer's stance, confidence and claimed basis. Ordinary recipient receipt can agree, conflict,
  or revise; the initial packet is not reread from later issuer beliefs.
- Executor learning acquired during a strategy advance keeps that operation's owner/local identity.
  A report carries attribution only for actually asserted claims whose current held record is still
  the recorded acquisition. The recipient stores the account only on delivery. Matching a target,
  any report by the same person, or private advancement alone cannot update the owner's operation
  status. A later unrelated source for the same claim loses the old attribution. Existing reporting
  vocabulary and ordinary receipt-triggered reviews are preserved.
- The planning text is a static multi-day expectation. The operation card shows the owner's ordered
  method and an attributed account/no-account status, while personal progress remains executor-only.
  Staffing explanations name only known people and the owner's own orders. Existing completion
  bookkeeping remains: the fact that an operation ended is visible as settled by milestone 026;
  private reasons for its ending are not thereby disclosed (`PlayerOccasion`'s existing boundary).
- Developer decision traces now include the separate executor alternatives and selected executor.
  The chosen-action signature and replay comparators include the new decision and attribution state.
  No fixture, personality, capability weight, resistance, interval, calendar or RNG algorithm was tuned.

## Verification — implementer evidence, not independent review

- `dotnet build CrimeEmpire.sln --no-restore`: **0 warnings, 0 errors**.
- `dotnet test CrimeEmpire.sln --no-build --no-restore`: **803 passed, 0 failed, 0 skipped**,
  including 23 new commissioning cases. Existing personal-start tests explicitly answer the new
  executor/confirmation controls; tests of the draft itself use raw public choices.
- Every seed-42 90-day variant repeated byte-identically with the final developer trace:

  | Variant | Trace hash |
  |---|---|
  | baseline | `D4CD8390355B55CB` |
  | cautious-vincent | `4B66626C3F1F7BCE` |
  | watchful-boss | `2FE30659C086A47D` |
  | disloyal-vincent | `B6977995C412723D` |
  | resentful-tommy | `49844ADF03714A7A` |
  | capable-angelo | `9C2D6EE27F864D09` |

- `--compare --seed 42`: **6 distinct traces, 5 distinct chosen-action sequences**.
  Required disloyal-Vincent/Salvatore and baseline/Vincent viewpoint runs exited zero.
- **24 Godot headless checks passed**, using Godot 4.7.1 Mono: the nine existing UI flags,
  `--selftest-commission`, the existing restart-save/restart-load pair, and six separate-process
  commissioning save/load pairs. Commissioning stages were `leaf` (executor not yet chosen),
  `executor` (chosen, confirmation pending), `back` (original occasion restored), `committed`,
  `execution` (three game days have elapsed), and `report` (Tommy's ordinary report is offered).
  Reports commit/deliver atomically, so the executor decision is the supported pre-delivery save
  boundary. The load process compared date, pending options/draft and player snapshot exactly,
  then exercised the restored choice. The in-process replay test additionally compares complete
  world fingerprints, including pending scheduling, commitments and attribution, at each boundary.
- The Godot runtime crashed before project initialization under the restricted execution environment;
  rerunning with normal runtime/user-settings access passed. This was not recorded as a game pass
  until the real project tests completed.
- `git diff --check` passed after removing an extra trailing blank line in an adapted test file.

### Deliberate fault checks

Each mutation was applied independently, required a failing relevant test, and was restored before
final verification. All twelve were detected:

1. Drop the initial delegated executor, creating a personal start.
2. Restrict automatic executor choice to self.
3. Discard the cached executor evaluation and prepare it again.
4. Retain a sixth delegate in addition to protected self.
5. Ignore held capability judgments when retaining delegates.
6. Append hidden target resistance to the planning expectation.
7. Copy the issuer's broader cognition into the initial briefing.
8. Treat an account for another operation as this operation's status.
9. Show the delegate's private live method to the owner.
10. Reschedule execution when an owner leaves orders unchanged.
11. Omit draft choices from the existing persistence input log.
12. Multiply first-stage operation candidates with executor-shaped copies.

Objective subordinate skill is structurally unavailable to the retention/scoring helper, which
receives actor view, perceived situation and existing psychology only. This boundary was inspected;
no claim is made that a fabricated world-truth skill lookup was itself mutation-tested. Information
boundary tests additionally cover unrelated reports, wrong recipients, later source changes, hidden
method/failure/phase, unknown work, and authoritative commit-time eligibility changes.

### Behavior changes and adapted historical assertions

The first natural baseline difference is at the initial executor choice: Vincent still chooses
Persuade at Ferri's tailor shop, then chooses Tommy immediately (executor totals 4.8442 versus
4.4637 for self at seed 42). A hands-free occasion lets him choose the grocery personally. The old
personal-start/later-handover script is still legal, but it is no longer the automatic history.
Whole-run actor parity remains separately checked through the shared automatic preferences.

Earlier delegation changes subsequent reports and review occasions without any tuning. The natural
baseline revenue-loss result is now 0.50 instead of 0.55; the objective remains unmet. Baseline,
cautious-Vincent, watchful-boss and resentful-Tommy now produce two perceived conflicts rather than
one. On day 20 Tommy's own operation card says he has pushed harder and been refused twice; the
owner sees his original standing method and no operation report yet. Historical assertions were
updated to these observed consequences, rather than restoring the earlier storyline with weights
or timing changes. A later-unrelated-report regression now composes a real report of a different
held claim, rather than assuming an obsolete automatic collection report arrives.

## Remaining gates and limits

- **No independent review has occurred for this implementation.** Test-green and implementer
  inspection do not record PASS, owner acceptance, or milestone closure.
- **Human comprehension has not been established.** Matt was asked to try the opening and describe
  the objective, executor, timing, known progress and unavailable staffing. No response has been
  recorded in this implementation. Automated text assertions are not a substitute.
- The owner rulings settle the commissioning scope. Pacing adequacy remains provisional under
  OPEN_CONCERNS #8; #9 is addressed by this implementation candidate but remains unclosed pending
  review and human evidence. Existing decision-RNG, calendar-engineering and tuning concerns remain.
- Milestone 030's retrospective independent audit remains carried work. The pre-existing ledger's
  milestone-030 implementation reference differs from this checkout's history (`5874077`); this
  implementation does not silently rewrite that separate authority record.
- New interventions, delayed commands, new refusal/negotiation behavior, live forecasts and new
  reporting vocabulary remain deferred. No next milestone is authorized by this delivery.
- Unrelated ROADMAP and proposal edits were left outside this commit. No push was requested or made.

## Correction 1 — preserve operation inputs during executor scoring

### Independent review and owner ruling

Sol's review supplied by Matt names exact implementation commit
`c279d04a7b65b802d359286cac8806eaf2b0303f` against parent `ae4e465`. Sol stated that he authored
neither commit. Verdict: **FAIL, Class A; safe to build upon: NO**. One new P1: commissioning
delegates used the executor as `TargetId` and dropped `RequiredKnowledge`, while generic reward
and uncertainty scoring interpreted those fields as the selected business and its evidence.
In the live capable-Angelo Bellini/Persuade trace, Tommy received reward +0.2156 and no uncertainty;
self received +0.3502 and -0.4226. These were supposed to price the same selected operation.

Sol reproduced a clean build, 803 passing tests, all six hashes twice, comparison (six traces/five
action sequences), both required viewpoints, all 24 Godot checks and the diff check. Sol did not
repeat the twelve source mutations because that review was read-only. Those original probes did
not cover this defect; their success was insufficient assurance. Other commissioning requirements
were assessed as implemented and covered, while human comprehension remained pending.

Matt accepted this P1 for correction. This append records that ruling and the focused correction;
it supersedes the original archive's claim that no independent review had occurred. It does not
change the original FAIL into PASS or establish acceptance of the corrected state.

### Correction and regression proof

- Initial delegate alternatives clone the retained operation, preserving its business, method,
  required knowledge and policy context. The staffing fields use the existing delegation crew
  requirement and do not require the commissioning actor's personal execution skill.
- `InitialExecutorId` carries the separate executor identity through retention, relationship and
  capability scoring, confirmation and presentation. Historical handover candidates keep their
  existing identity convention through the scorer's fallback. No new coefficient is introduced.
- The behavioral replay comparator includes the now-separate executor identity. Existing retention
  assertions read that identity rather than the business target.
- Six additional cases use the production commissioning preparation path: an unrelated vulnerability
  belief about Tommy leaves executor scores unchanged; changing shop vulnerability or required-claim
  confidence affects every alternative's corresponding component; changing only relationship or held
  capability evidence can reorder delegates without changing those operation components; delegated
  force preserves the selected operation's policy cost.
- Temporarily injecting `TargetId = id` into the new delegate construction made
  `Executor_scoring_ignores_unrelated_vulnerability_of_the_executor` fail (one test, one failure).
  Source bytes were restored in a finally block, then rebuilt before all verification below.

### Verification on the restored correction

- `dotnet build CrimeEmpire.sln --no-restore --verbosity quiet`: 0 warnings/errors.
- `dotnet test CrimeEmpire.sln --no-build --no-restore`: **809 passed**, no failures/skips;
  29 commissioning cases, including the six additional cases. No historical expected outcome was
  changed to make this correction pass.
- `--verify --seed 42 --days 90` for every existing variant produced equal A/B hashes:

  | Variant | Corrected trace hash |
  |---|---|
  | baseline | `A99EC8C3720272B5` |
  | cautious-vincent | `F5890C72AAA69948` |
  | watchful-boss | `2A05F3A500CF0D74` |
  | disloyal-vincent | `ED344BD31EF7B2E6` |
  | resentful-tommy | `A4D7CEA8BE62B8C7` |
  | capable-angelo | `F5C95925301DA227` |

- `--compare --seed 42`: six distinct traces, five distinct chosen-action sequences. Hashes change
  because executor evaluation now uses the actual operation inputs. No timing, RNG algorithm,
  personality, capability weight, fixture or pacing tuning was performed.
- Required disloyal-Vincent/Salvatore and baseline/Vincent viewpoint runs completed successfully;
  the existing information-boundary tests also passed. This is implementer verification.
- All 24 headless Godot checks passed: the nine existing single-process checks, commissioning,
  restart save/load, and commissioning save/load pairs at leaf, executor, back, committed, execution
  and report boundaries. An isolated ordinary-save override was supplied; the real playtest save's
  hash was identical before and after the run.
- Logs were written to temporary `ce031-correction-*` files. Saves remain build-specific under the
  existing replay contract; this is not a claim that saves from the previous build load in this one.

### Human playtest evidence and carried feedback

Before this correction, Matt confirmed direct Tommy commissioning and confirmation, busy-Tommy
exclusion with personal execution of the other job, multi-day progression, personally collected
tailor income followed by Tommy's grocery income, and save/load at various phases with no duplicate
payment and stable executor/job/method. His initial load complaint was explicitly retracted.

Matt requested clearer mission wording identifying both shops without losing source attribution;
permission wording that states the requested act; meaningful visible responses to reports and
questions; relevance checks for asking about a business after payment; and the ability to tell a
delegate to change approach. The screenshots also show a vertically wrapped session-objective
label occupying excessive space. These are observations for later scope assessment, not changes
made in this P1 correction. A pending answer is not by itself proof that a response never occurs;
stale character beliefs are not automatically an information-boundary defect. Suggested dialogue
must not invent private feelings, progress, forecasts or a financial-cut mechanic.

New owner intervention remains explicitly deferred under the authorized M031 scope. The full human
comprehension gate, independent correction review and Matt's acceptance of the corrected state are
not established by these tests or observations. Unrelated ROADMAP/proposal work remains untouched.

## Owner closure — 2026-09-21

Matt supplied Sol's independent review of exact correction
`80037423f7760e5fe8f828b5a559f8af4e1e45fc` against
`c279d04a7b65b802d359286cac8806eaf2b0303f`: **PASS AFTER FIXES, Class A; safe to build upon: YES**.
Sol stated that he authored neither commit. No new P1 or P2 findings remained. The accepted P1 was
verified fixed across retention, scoring, confirmation, presentation and replay comparison, with
historical delegation preserved through the fallback. The live Bellini/Persuade trace gave both
Tommy and Vincent reward +0.3502 and uncertainty -0.4226.

Sol reproduced the clean build, 29 focused and 809 total passing tests, all six repeated variants
and recorded hashes, six-trace/five-action-sequence comparison, required viewpoints, all 24 Godot
checks, and exact diff/whitespace checks. Sol did not independently mutate the source; he inspected
the load-bearing test structure. That limitation does not become a claimed mutation rerun here.

Matt then playtested the corrected build. He again noted the absent ability to change Tommy's
approach, and that the shops never explained why payment took so long. He explicitly described the
latter as a note for future narrative work. He confirmed both incomes: +620 from Ferri handled
personally and +840 from Bellini handled by Tommy, taking cash from 6,000 to 7,460 with no ongoing
operations shown. His earlier save/load checks and stable executor/job/method observations remain
as recorded above; they were not claimed as repeated in this final short run.

After those observations Matt ruled: "great. I think Milestone 31 is closed." This records his
acceptance of the corrected state and the milestone's human-comprehension gate, following the
independent PASS. It supersedes the pending-gate statements above without rewriting their history.
It is not a claim that every narrative or interface issue is resolved, nor a separate measurement
of every comprehension criterion.

Carried feedback includes explanations for delays, visible conversational responses, clearer
mission/permission wording, post-completion question relevance, the oversized vertically wrapped
session-objective label, and owner control over a delegate's method. New intervention mechanics,
delayed commands, refusal/negotiation, live forecasts and reporting vocabulary remain deferred.
OPEN_CONCERNS #8 remains open for pacing/narrative legibility; #9's initial delegation and staffing
gaps are resolved. The M030 retrospective review and historical hash discrepancy remain carried.

This closure changes documentation only. Runtime verification above is attributed to the named
reviews; it was not rerun for prose edits. The closure commit requires independent Class B review
under the ordinary process; Matt did not grant an exception. No next milestone was begun, no push
was made, and unrelated ROADMAP/proposal work was left untouched.
